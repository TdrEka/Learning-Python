a = (1, 2, 3)
b = tuple([4, 5, 6])

print(a)
print(b)

# they're better optimized than lists.
# They cant be altered
# they can be accessed the same way as lists via index [0 to range]
# they are created using '()'
# they can have all kinds of data inside and different kinds in the same tuple

